name: Memory Benchmark

# STUB — kept for filename parity with
# CIRISAgent/.github/workflows/memory-benchmark.yml.
#
# CIRISAgent's memory-benchmark runs the long-horizon-memory regression
# tests against the persistence tier. The CIRISGame analogue would be the
# WASM heap / animation-frame allocator regression (does a 27-cell starter
# game leak across 1,000 moves?). When the game has enough surface to
# regress, replace this stub with the cargo bench + heaptrack pipeline.

on:
  workflow_dispatch:
